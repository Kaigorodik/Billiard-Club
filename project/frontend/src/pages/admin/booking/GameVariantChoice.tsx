import {styled} from "@mui/material/styles";
import Dropdown from "../../../components/Dropdown";

const GameVariantChoice = styled(Dropdown)(({theme}) => ({
    color: 'black',
    background: 'none'
}));

export default GameVariantChoice;